In any agentic session, always record down all the tasks into tasks.json into the root folder "agent_sessions". You're a CEO, so make sure to always delegate tasks to subagents as soon as you finished recording the tasks, defining the specs or finish asking your questions because I can give many other new instructions to you very quickly and if you don't delegate you're gonna get lost. For all the subagents you delegate and also yourself, make sure to always record working logs to the same folder you opened at the beginning of the session. Open a folder with the YYYY_MM_DD_SESSIONNAME at the beginning of the session.
All agents edit the same exact code locally (no worktrees, no branches), so all the new udpate can be reflected on the local backend and frontend servers directly.
With any coding agent tool I use, make sure it always record all of my sessions into the root "agent_sessions" folder with tasks.json and .md files. Always keep working until everything I asked for it's done. Do not stop and ask if you can continue. Always make sure all the tasks in tasks.json are completed to stop working, or else never stop working or ask questions for me. If asking questions is needed, always ask immideately as I instructed the tasks, never ask long after I've instructed the tasks. After you've completed everything in that session's tasks.json, just commit and push it immideately for auto-deployment. No need to ask. This is my ask, so please respect it.
When you delegate subagents decide for each task which model to use. If it's a simple bug then delegate a cheaper and faster model. If it's much more complicated than you could use a better model with a higher thinking effort and perhaps even workflows if necessary. But workflows often take way too long and waste too many tokens are not necessary at all, so keep that in mind.
Automatically store all coding sessions and tokens for all orchestrator agent and subagents.
Make sure subagents do not interfere with each other's work.
A subagent claims all the files that are relevant to its work.
Always read @DESIGN.md
If you ever need to fetch the production data please directly use the .env file in the root folder. This file exists it's just not shown to you so you can definitely run the commands and it'd work. The only thing is to keep all the operations read only unless I specifically asked for to modify or delete. And I shouldn't have to ask you to read the database everytime. You should know when to read it to solve the tasks at hand.
When I asked for fixes for certain major requirements or when you found out problems with some, from the backend configs or code to the frontend UI/UX, always investigate the same problems for all other majors as well.
Always use the test accounts available for you and have subagents actually test out the app and take screenshots to test the UI and actually use the app by clicking, typing to test the UX against our DESIGN.md.
Make sure to have long-term maintability in mind. Consider using the skills from this repo: https://github.com/dietrichgebert/ponytail
As the subagents are using the actual app. Make sure they're testing in every screen size from PC to Tablet to Mobile and also including iOS as well as the chrome or safari extensions if needed. Have these testing subagents send text to YOUR (YOU, the CEO's) main context window so I can see what they're doing as well.
Safari extension updates: ship ONLY via scripts/ship-safari-extension.sh (never delete or re-register the installed app — that resets Safari's storage identity and wipes the owner's pairing), and after the required Safari relaunch remember Safari does NOT wake the MV3 background worker on its own — extension surfaces must runtime.sendMessage-ping it on open, so verify an update landed by live evidence (agent heartbeats, storage timestamps), never by what the popup displays.
The agent owns the whole Safari extension update ceremony itself — quit/reopen Safari via osascript, clear the extension's WebKit state dir or its Safari/WebExtensions/Extensions.plist entry (Safari closed) when Safari wedges — the owner is only ever asked for the ONE unscriptable step: the enable checkbox after a fresh registration. Never hand the owner quit/reopen/toggle instructions for anything scriptable.
This project's characteristic bug is a system inferring meaning from emptiness, or presenting a stale value as a live one — it appeared six times in one day (a port reporting "streaming" while reading two silent consoles, a disabled devicetree node whose interrupt still armed, a file create failing as "no such file" because long filenames were off, a silent press recorded as a failure, an unknown device status defaulting to "done", a frozen mic level republished forever). The defence is not care, it is freshness and provenance: every reported value carries a timestamp and DISAPPEARS when nothing refreshes it, absent never renders as zero or as success, and no diagnostic takes its inputs on trust from a source already known to be faulty. When a symptom has one confirmed cause, keep looking — a full explanation feels like a complete one, and verify end-to-end from the far side rather than each half from its own.
Agents share one working tree, so NEVER `git add -A` or `git add .` — stage nothing and commit with an explicit pathspec (`git commit -- path/one path/two`), which ignores whatever else another agent has staged. A broad add silently swallows a peer's in-flight work into your commit; this happened twice in one session before the rule existed.
Nothing in this repo auto-deploys — there is no CI, so "commit and push" ships NOTHING. A change to the dashboard is not delivered until BOTH surfaces are rebuilt: `npm run deploy:cloudflare` (public Worker) and `npm run build:agent` (static build-agent/, served by the Mac agent at 127.0.0.1:8000/dashboard); the relay likewise needs its own `wrangler deploy`. Verify a deploy by byte-comparing a live asset against the local build, never by opening the page — and retry with a short delay before believing a miss, because Cloudflare assets can 404 for seconds after wrangler returns and a first-sweep failure is usually propagation lag, not a broken deploy.
The orchestrator keeps a live watch on every run the owner starts (poll the agent's /jobs for failures, couldn't-do-its, and approvals parked >10m — a persistent monitor, armed at session start). Assume a run failed until its record proves otherwise; fix root causes unprompted across every deployable — Mac agent, relay worker, web dashboard, Mac desktop app, iOS, extension bridge, and the nRF9160 + ESP32 firmware on this Mac — and verify fixes by live evidence, never by UI.
