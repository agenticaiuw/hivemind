# Harness derivation — faculty-perception — round 85

Model: `gpt-5.6-luna`  ·  probes against `http://localhost:8000`

## What it established

- **live-machine-state-2026-08-07-round85** — At 2026-08-07T13:33Z the Mac bridge is online, but browser extension home-chrome is offline with 9 pending browser commands. /observe reports 3 durable browser sessions (default UTC tab, probe-form2 Selenium form, probe-form httpbin) even while the extension has no attached tab. Accessibility and Screen Recording are false; AppleScript automation grants are present. /pipeline contains at least one run stuck in processing from 07:22 and another completed run whose command was blocked awaiting approval, so pipeline state is not equivalent to deliverability or execution completion.
  - evidence: GET /ops/status, GET /observe, GET /browser/status, GET /pipeline all returned HTTP 200 at 2026-08-07T13:33Z.

## Capabilities it proposed

### "“Before I rely on it, tell me what parts of my AI system are really reachable right now, what is merely queued or stale, and what evidence supports each answer.”"
- **useful because:** Today the fleet reports contradictory truths: the Mac bridge is online while its browser extension is offline with nine pending commands; durable browser sessions remain listed without an attached tab; and pipeline records can remain processing or approval-blocked for hours. A perception-first reachability brief prevents the owner from believing an action happened, or exposing private browser work to a nonexistent connection. It is genuinely cross-node: only the relay can compare delivery records, the Mac can inspect local sessions and permissions, and the browser can confirm attachment.
- **path:** relay-realtime → mac-planner → browser-extension → unified → faculty-perception
- **model tier:** background: deterministic reconciliation and a cheap text model for the brief; realtime only when the owner asks verbally for an immediate status.
- **latency:** Under 2 seconds for cached status; up to 10 seconds for fresh Mac/browser heartbeats. Never claim fresh evidence if a surface does not answer.
- **cost:** Usually near-zero model cost for typed status aggregation; roughly $0.001–$0.01 only when a short model-generated explanation is requested. Main cost is context transfer from status records, capped to changed records.
- **security:** Do not include private tab URLs, page text, or tokens in the spoken brief unless explicitly requested. Report capability class and evidence timestamps. Pending browser commands must be labeled and never replayed by this read-only capability. Require confirmation before any attempted reconnect or retry.
- **missing:** A typed cross-surface reconciliation record with observedAt, source, freshness TTL, and state vocabulary (reachable, attached, queued, executing, delivered, approval-blocked, stale, unknown).; A relay endpoint that joins Mac job/pipeline records with browser heartbeat/command state and device registry without copying page contents.; Browser extension reconnect/heartbeat implementation; currently home-chrome is offline.; Pendant registration and delivery acknowledgements; no pendant is currently registered.; Owner-granted Accessibility and Screen Recording for com.aipendant.agent if GUI reachability is to be reported as ready.

### "“Show me a tamper-evident timeline of what I said, what each node received, what it decided, what it tried, and what reached me—including gaps while I was offline—and let me export that incident report.”"
- **useful because:** Today a stale pipeline record, a relay acknowledgement, a Mac receipt, and a pendant playback can look like the same event even when they are not. The owner cannot independently answer whether a spoken request was transcribed, acted on, delivered, played, or lost during a disconnect. A causal, gap-aware timeline gives them a trustworthy answer after the fact without silently retrying or changing anything. This is not a task runner, page watcher, briefing, or generic job-status view: it is an evidence export spanning the wearable, relay, Mac, and browser, including explicit unknown intervals.
- **path:** relay-realtime → mac-planner → browser-extension → unified → faculty-perception
- **model tier:** Deterministic event stitching and hashing first; use a cheap background text model only to render a human-readable incident summary. Realtime is unnecessary.
- **latency:** Export a recent incident in under 3 seconds from indexed records; historical reconstruction may take 10 seconds. Missing evidence must be shown as a gap, never inferred.
- **cost:** Near-zero inference cost for the structured timeline; approximately $0.001–$0.01 for optional prose. Storage and indexing of compact event metadata dominate, not model calls.
- **security:** The export may contain private speech transcripts, URLs, action parameters, and device identifiers. Encrypt at rest, scope access to the owner's bearer/session, redact payloads by default, offer a metadata-only export, and require explicit confirmation before sharing or uploading an incident report. Hashes prove record continuity, not truth of an untrusted source, so show source and trust level.
- **missing:** A cross-node event envelope with globally unique event IDs, parent/causal IDs, monotonic sequence numbers per device, source identity, observed time, received time, and explicit event type.; Relay-issued connection epochs and delivery/playback acknowledgements binding a response to a registered device; late, duplicated, simulated, and historical events must be distinct states.; Mac and browser adapters that append signed/hashed receipts rather than only human-readable job status; browser tab/session identifiers must be privacy-redactable.; An append-only encrypted event store plus a verifier/export route that preserves missing intervals and detects duplicates, rewrites, and clock disagreement.; A user-facing incident viewer/audio transcript redaction workflow on the Mac dashboard and a concise spoken answer on the relay.


## Changes it proposed to its own stack

### `integration` — Add a read-only fleet truth ledger and reconciler. Every observation from relay device registry, Mac /ops/status and /observe, browser heartbeat/poll, jobs/receipts, and pipeline events becomes a typed fact {surface, objectId, state, observedAt, source, evidenceHash, expiresAt}. Reconcile contradictory states without overwriting evidence: e.g. browser session=present but extension=offline => session detached; pipeline=processing beyond TTL => stale, not running; local action=approval-blocked => not delivered. Expose a compact diff endpoint and emit a change event only when a fact crosses state or freshness boundaries.
- **owner gets:** The owner gets honest answers about whether something is reachable, queued, actually executed, or merely an old record. It stops the most dangerous failure mode in a personal assistant: a success-looking receipt when the screen was never touched or a pendant never received the result.
- effort: Medium: shared schema, relay D1 tables/queries, Mac adapter, browser heartbeat adapter, TTL policy, tests with offline and delayed-delivery fixtures. No GUI permission required for the read-only ledger.  ·  risk: Incorrect TTLs could label slow work stale or create noisy transitions. Preserve raw observations, show evidence timestamps, use conservative unknown after expiry, and make the ledger read-only so reconciliation cannot trigger retries or actions.
- cost: Negligible storage/compute per event; a few D1 rows per state transition. No model call for reconciliation; optional summary generation is low-cost background text.  ·  latency: Cached reads under 100 ms; fresh reconciliation adds one heartbeat interval, normally 1–3 seconds. It should never delay execution paths.
- security: Store hashes and metadata by default, not private page text or audio. Restrict the ledger to the owner bearer token; redact URLs/titles in spoken output unless requested.
- depends on: A single authoritative device-registry contract and delivery acknowledgement semantics.; Stable job/pipeline IDs and idempotent event ingestion.; Browser extension heartbeat/reconnect; currently extension is offline.; Owner Accessibility/Screen Recording grants only if the ledger is later extended to assert GUI input reachability.

### `relay` — Add a live-origin gate to relay and Mac pipeline records: each event carrying source=nrf9160/pendant must include a registered deviceId, connection epoch, and delivery acknowledgement. If the device is absent or the event predates its last-seen epoch, retain it as historical replay but expose originStatus=historical/unverified rather than live. Prevent dashboards and judgement from treating recorded audio or held-alert events as current pendant state.
- **owner gets:** The owner will not be told that a pendant received or can play something when no pendant is registered. Historical test recordings remain useful for debugging, but cannot masquerade as a live wearable.
- effort: Small-to-medium relay schema/API change plus Mac pipeline display mapping and fixtures for offline/reconnect epochs.  ·  risk: Clock skew or delayed LTE uploads could be mislabeled historical. Use relay receipt time plus connection epochs, allow an explicit late-delivery state, and never delete raw events.
- cost: Minimal D1 metadata per event; no model cost.  ·  latency: Negligible on live ingestion; one registry lookup or cached epoch.
- security: Reduces false claims about device delivery. Device IDs and epochs are metadata; do not expose audio contents in status.
- depends on: Authoritative relay device registry and delivery acknowledgement semantics (currently still missing from my available context).; A distinction between test/simulated and production device identities.; Existing pipeline event IDs and durable audio metadata.

### `integration` — Create a cross-node causal event journal distinct from execution receipts: pendant, relay, Mac, and browser each append hash-linked envelopes with local monotonic sequence, parent event ID, connection epoch, observed/received timestamps, and trust class. The relay periodically seals batches; a verifier detects missing ranges, duplicate IDs, clock conflicts, and post-hoc edits, then produces a redacted incident export with explicit UNKNOWN gaps. Keep payloads encrypted and optional; metadata-only verification must work.
- **owner gets:** After a disconnect or surprising action, the owner can prove what actually happened and where the chain broke instead of choosing between contradictory status screens. They get an honest incident report, including “no evidence of playback,” rather than a confident but false success.
- effort: High: protocol/schema shared by firmware source, relay D1, Mac agent, and browser bridge; append-only storage, key rotation, export verifier, dashboard viewer, and simulated loss/duplication tests.  ·  risk: More metadata can expose behavior patterns and clock conflicts can create confusing reports. Default to local encryption and redaction, retain raw evidence only for a configurable period, label source trust explicitly, and never let verification trigger retries or actions.
- cost: Small per-event storage/index cost and occasional batch hashing; no model cost. Larger exports may incur encrypted storage/network cost only when the owner requests them.  ·  latency: Append is asynchronous and should add under 20 ms to normal processing; sealing/export is background. No impact on realtime speech path.
- security: Improves tamper detection but introduces signing keys and sensitive event metadata. Keep keys device-scoped, rotate/revoke on pairing changes, prevent the relay from decrypting payloads by default, and require owner confirmation for any remote export.
- depends on: A stable pairing identity and connection-epoch protocol for the future pendant.; Relay persistence and authenticated Mac/browser event ingestion.; A defined retention/redaction policy and owner-visible verifier UI.; The currently absent physical pendant must be connected before pendant-side signatures and playback evidence can be validated.


## What it asked for

_Nothing._
## Its own summary

Round 85 established fresh, actionable truth: Mac bridge is online; browser extension is offline with 9 pending commands; durable browser sessions/tabs remain recorded despite no attached browser; Accessibility and Screen Recording are false while AppleScript grants are true; pipeline contains stale processing and approval-blocked records. I recorded this, informed faculty-judgement, and proposed (1) a cross-surface reachability brief, (2) a typed fleet truth ledger with freshness/contradiction handling, and (3) a relay live-origin gate so historical nRF9160 records cannot masquerade as live pendant delivery. What I still need is not another local probe: authoritative relay device IDs, connection epochs, and delivery acknowledgements; browser extension reconnection; and, if GUI claims are needed, owner-granted TCC permissions for com.aipendant.agent. There is no pendant live now, so pendant behavior remains unverified.

**Biggest unknown:** Whether relay has authoritative device connection epochs and delivery acknowledgements that can distinguish live, delayed, simulated, and historical pipeline events.

